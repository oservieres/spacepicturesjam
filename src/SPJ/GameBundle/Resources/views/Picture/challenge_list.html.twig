{% for picture in pictures %}
    <img class="img-thumbnail" src="{{ asset(challenge.status == 'inprogress' ? picture.blurredMiniaturePath : picture.miniaturePath) }}" alt="{{ picture.title }}"/>
{% endfor %}
