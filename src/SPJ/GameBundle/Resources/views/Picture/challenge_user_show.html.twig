{% if picture %}
    <a href="{{ asset(picture.path) }}" target="_blank"><img src="{{ asset(picture.miniaturePath) }}" alt="{{ picture.title }}"/></a>
{% else %}
    You did not participate
{% endif %}
