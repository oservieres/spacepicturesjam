<div class="create">
    {% if app.user == "" %}
        <div class="alert alert-danger">
            <a href="{{ path('login') }}">{{ "picture_comment_login"|trans() }}</a>
        </div>
    {% else %}
        <form method="POST" action="{{ path('comment_create', { pictureId : picture.id }) }}">
            <div class="input-group">
                <textarea name="content" class="form-control content" placeholder="Rédigez un commentaire..."></textarea>
            </div>
        </form>
    {% endif %}
</div>
